# homebrew-tap

Homebrew tap for Vaayne apps.

## Install

```bash
brew tap vaayne/tap
brew install --cask mori
```

## Available casks

- `mori`

## Maintenance

Each cask should point at a tagged GitHub Release asset with a fixed SHA-256.
